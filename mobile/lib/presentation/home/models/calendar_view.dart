// Vista del calendario (giorno/settimana/mese/anno) e campo orario in modifica.

enum CalendarView { day, week, month, year }

enum CalendarTimeField { start, end }

CalendarView calendarViewOrDefault(CalendarView value) => value;
