version: "2.1"

services:
  api:
    image: ${API_IMAGE}
    env_file:
      - .env
    volumes:
      - ./updates:/app/updates
      - ./tickets:/app/tickets
    ports:
      - "8080:8080"
    expose:
      - "8080"
    restart: unless-stopped
